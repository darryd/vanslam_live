<% # image_tag image_path "logo_slam.png" %>
<% # FIXME: image_tag just displays a blank image. %>
	
<h1> <%= @title %>  </h1>

<div id="create_new_slam" class="vwli">
  <div id="template_slams"></div>
</div>

<% @slams.each do |slam| %>
  <h2> <%= link_to slam.title, "/competition/show?id=" + slam.id.to_s %> </h2>
<% end %>
